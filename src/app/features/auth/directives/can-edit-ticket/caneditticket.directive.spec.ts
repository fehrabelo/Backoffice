import { CaneditticketDirective } from './caneditticket.directive';

describe('CaneditticketDirective', () => {
  it('should create an instance', () => {
    const directive = new CaneditticketDirective();
    expect(directive).toBeTruthy();
  });
});
